<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:windowBackground">@color/Background</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    <style name="Normal_TextSize">
        <item name="android:textSize">?attr/normal_text_size</item>
    </style>
    <style name="Attention_TextSize">
        <item name="android:textSize">?attr/attention_text_size</item>
    </style>

    <style name="Small_textSize">
        <item name="normal_text_size">15dp</item>
        <item name="attention_text_size">20dp</item>
    </style>

    <style name="Middle_textSize">
        <item name="normal_text_size">20dp</item>
        <item name="attention_text_size">25dp</item>
    </style>

    <style name="Big_textSize">
        <item name="normal_text_size">30dp</item>
        <item name="attention_text_size">35dp</item>
    </style>

    <style name="Huge_textSize">
        <item name="normal_text_size">40dp</item>
        <item name="attention_text_size">45dp</item>
    </style>
</resources>